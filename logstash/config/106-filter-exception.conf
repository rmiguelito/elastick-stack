filter {
	if [message] =~ /.*Exception.*/ {
		grok {
			match => [ 
				"message", "^(?<excecao>[a-zA-Z0-9.]+):?.*"
			] 
		}

		mutate {
			add_field => [ "event_type", "exception" ]
			add_tag => ["processado"]
		}
	}
}