filter {
	if [message] =~ /.*ConsultaPorNome - ComunicacaoInterceptada null.*/ {
		grok {
			match => [ 
				"message", "%{TIMESTAMP_ISO8601:[@metadata][timestamp]}.*"
			] 
		}

		mutate {
			add_field => [ "event_type", "error_update" ]
			add_tag => ["processado"]
		}
	}
}